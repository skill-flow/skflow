import type { DoneResult } from "./protocol.js";

// Placeholder for script authors — the transformer converts this into a terminal return.
export function done(result: DoneResult): DoneResult {
  return result;
}
